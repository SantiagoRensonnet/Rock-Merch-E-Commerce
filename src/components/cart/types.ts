type Item = {
  id: number;
  name: string;
  price: number;
  imageUrl: string;
  imageXOffset?: string;
  imageYOffset?: string; //Y offset from centered position
  qty: number;
};
export type { Item };
