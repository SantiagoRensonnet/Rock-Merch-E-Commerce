interface CategoryData {
  id: number;
  title: string;
  imageURL: string;
}
interface CategoryUI {
  id: number;
  title: string;
  imageURL: string;
  style: string;
}
export type { CategoryData, CategoryUI };
